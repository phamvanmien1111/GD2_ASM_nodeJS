{{!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Đăng Nhập</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
  <div class="flex justify-center items-center min-h-screen">
    <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
      <h2 class="text-2xl font-bold mb-6 text-center">Đăng Nhập</h2>
      {{#if error}}
        <p class="text-red-500 text-center">{{error}}</p>
      {{/if}}
      {{#if successMessage}}
    <div class="alert alert-success text-blue-900">{{successMessage}}</div>
      {{/if}}
      <form action="/dangnhap" method="POST">
        <div class="mb-4">
          <label class="block text-gray-700 font-medium mb-2" for="username">Tên đăng nhập</label>
          <input type="text" id="username" name="username" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <div class="mb-4">
          <label class="block text-gray-700 font-medium mb-2" for="password">Mật khẩu</label>
          <input type="password" id="password" name="password" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <button type="submit" class="bg-blue-500 text-white w-full py-2 rounded-lg hover:bg-blue-600 mb-4">Đăng Nhập</button>
      </form>
      <div class="text-center">
        <a href="/dangky" class="text-blue-500 hover:text-blue-700">Đăng ký</a>
        <span class="mx-2 text-gray-400">|</span>
        <a href="/quenmatkhau" class="text-blue-500 hover:text-blue-700">Quên mật khẩu?</a>
      </div>
    </div>
  </div>
</body>
</html> --}}
{{!-- <body class="bg-gray-100">
  <section id="login" class="min-h-screen flex flex-col justify-center items-center">
    <div class="bg-white p-10 md:p-16 shadow-md shadow-gray-300 rounded-lg w-[90%] md:w-fit">
      <h2 class="text-2xl font-bold mb-6 text-center">Đăng Nhập</h2>
      {{#if error}}
        <p class="text-red-500 text-center">{{error}}</p>
      {{/if}}
      {{#if successMessage}}
        <div class="alert alert-success text-blue-900">{{successMessage}}</div>
      {{/if}}
      <form action="/dangnhap" method="POST" class="grid grid-cols-1 gap-4 text-sm">
        <div class="flex flex-col">
          <label class="text-gray-700 font-medium" for="username">Tên đăng nhập</label>
          <input type="text" id="username" name="username" class="border p-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <div class="flex flex-col">
          <label class="text-gray-700 font-medium" for="password">Mật khẩu</label>
          <input type="password" id="password" name="password" class="border p-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <div class="flex justify-between items-center">
          <div class="flex items-center gap-2">
            <input type="checkbox" id="remember" name="remember" class="w-4 h-4">
            <label for="remember" class="text-gray-700">Ghi nhớ đăng nhập</label>
          </div>
          <a href="/quenmatkhau" class="text-blue-500 hover:underline">Quên mật khẩu?</a>
        </div>
        <button type="submit" class="bg-blue-500 text-white text-base py-2 rounded-lg hover:bg-blue-600">Đăng Nhập</button>
      </form>
      <p class="text-center mt-4">
        Chưa có tài khoản?
        <a href="/dangky" class="text-blue-500 hover:underline">Đăng ký</a>
      </p>
    </div>
  </section>
</body> --}}
<body class="bg-gray-100 flex justify-center items-center min-h-screen bg-cover bg-center" 
style="background-image: url('https://i.pinimg.com/736x/b6/5e/4b/b65e4b2ccfa2f8872d86725924add187.jpg');">
  <div class="flex flex-col items-center w-full max-w-6xl p-8">
    <!-- Tiêu đề -->
    <h1 class="text-5xl font-bold text-white mb-10 text-center">Đăng Nhập</h1>
     {{#if error}}
        <p class="text-red-500 text-center">{{error}}</p>
      {{/if}}
      {{#if successMessage}}
    <div class="alert alert-success text-white">{{successMessage}}</div>
      {{/if}}
     <div class="flex w-full ">{{!--  /images/z6314596800112_1755e22fcdd304665eb39139cfcbf465.jpg--}}
      <div class="w-1/2 flex justify-end">
        <div class="w-[600px] h-[450px] bg-gray-300 rounded-l-xl   overflow-hidden shadow-lg">
          <img src="https://atpro.com.vn/wp-content/uploads/2024/05/forum-la-gi.jpg " 
               alt="User 1" 
               class="w-full h-full object-cover">
        </div>
      </div>
      <div class="w-1/2 flex justify-start">
        <div class="bg-white/70 p-8 rounded-r-xl shadow-lg w-[500px] h-[450px] flex flex-col justify-center">
          <form action="/dangnhap" method="POST">
            <input type="text" placeholder="Email hoặc số điện thoại" name="username" 
                   class="w-full p-4 border rounded-lg mb-4 focus:ring-2 focus:ring-blue-500">
            <input type="password" placeholder="Mật khẩu" name="password" 
                   class="w-full p-4 border rounded-lg mb-4 focus:ring-2 focus:ring-blue-500">
            <button type="submit" class="w-full bg-blue-600 text-white p-4 rounded-lg hover:bg-blue-700">
              Đăng nhập
            </button>
            <a href="#" class="block text-center text-blue-500 mt-3">Quên mật khẩu?</a>
          </form>
          <hr class="my-6">
          <button class="w-full bg-green-500 text-white p-4 rounded-lg hover:bg-green-600">
            <a href="/dangky" class="text-white hover:underline">Tạo tài khoản mới</a>
          </button>
        </div>
      </div>
    </div>
  </div>
</body>
